document.addEventListener('DOMContentLoaded', function() {
    // This JavaScript file can be kept as it is
    // Note: The href in the login.ejs already points to the correct route (/auth/login)
    
    // Add any client-side functionality here if needed
    console.log('LINE Login app initialized');
});